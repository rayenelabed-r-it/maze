"""Suite de tests du projet Amazing-Mazes.

Ce fichier existe pour que ``tests`` soit un paquet importable : plusieurs
modules de test partagent les constantes definies dans ``tests/conftest.py``
(``TEST_SEED``, ``SMALL_SIZES``, ``LARGE_SIZES``) et les importent par
``from tests.conftest import ...``. Sans ce fichier, l'import echouerait selon le
repertoire depuis lequel pytest est lance.

Voir ``tests/conftest.py`` pour la strategie de test et les fixtures partagees.
"""
